# uidcore
**Project path:** /var/www/uidcore
**Stack:** (unable to detect from project files)
**Key files:** (unable to detect from project files)
## Project Context Snapshot
```md
# Project: uidcore
## File Structure
```
└── README.md
```
## README (first 20 lines)
# UidCore — Template Financeiro Multi-Nicho
> Sistema-template (núcleo/backbone) para os produtos verticais da Uid Software — modelo ISV/SaaS por nicho (pilates, salão, loja, clínica etc.), com módulo financeiro "CFO as a Service" para MEI e pequenas empresas.
---
## Visão Geral
Sistema-template (núcleo/backbone) para ser a base de todos os futuros produtos verticais da Uid Software (modelo ISV/SaaS por nicho: pilates, salão, loja de roupa, clínica etc. — já validado com o Studio Fluir em produção). A ideia partiu do próprio fundador (Luiz) ao perceber que módulos como cadastro de clientes, fornecedores, financeiro, vendas, pagamentos, administrativo, funcionários/RH, agendamento e área do usuário se repetem em praticamente todo nicho atendido. Construir esse núcleo uma vez, bem feito, e adaptar por segmento — em vez de reconstruir do zero a cada novo cliente.
## Módulos Comuns
- Cadastro de Clientes (CRM básico: dados, histórico, segmentação)
- Cadastro de Fornecedores (dados, CNPJ, contato, categorias de fornecimento)
- Vendas (pedidos, orçamentos, propostas, funil comercial simplificado)
- Pagamentos (recebimento de clientes, integração com meios de pagamento — PIX, boleto, cartão)
- Administrativo (documentos, contratos, processos internos)
- Funcionários / RH (cadastro, folha de pagamento básica, férias, admissão/demissão)
- Agendamento (controle de compromissos/horários — módulo genérico reaproveitável por clínica, salão, estúdio, prestador de serviço em geral)
- Área do Usuário / Portal do Cliente — acesso próprio pro cliente final (perfil separado de admin/operacional) ver seus próprios dados: faturas, agendamentos, histórico, chamados/suporte — sem precisar entrar em contato pra tudo. Segue o mesmo padrão que já existe no SystemD (perfil CLIENTE: MeusProjetos, Suporte, MinhasFaturas).
```
This file was auto-generated by Claw Empire to provide project context.
---

## Histórico de execuções

### [2026-08-06] — Manutencao #24 — 3 bugs de frontend (dropdown overflow, nome_razao_social, grid pagamento)

**Tarefas executadas:**
- Fix overflow de dropdown (conteúdo cortado/escondido em componente de seleção)
- Fix relacionado ao campo `nome_razao_social`
- Fix no grid de pagamento (layout)

**Commits:**
- 7aa7fc2

**Sentinel:**
- 182/182 testes passando (0 falhas)
- 8/8 critérios de aceite verificados
- Status: APROVADO

**Deploy:**
- Data: 2026-08-06 00:17:53 -03
- Metodo: git push origin main → CI/CD GitHub Actions (.github/workflows/deploy.yml)
- Push confirmado: commit 7aa7fc2 em origin/main
- Container uidcore-backend-1 recriado as 2026-08-06 00:17:53 -03 — deploy confirmado
- SystemD: Manutencao #24 marcada feito=True via disparar_hotfix --concluir 24
- URL: http://uidcore.uidsoftware.com.br (porta interna 8006)
- Status: ✅ Aprovado por Sentinel / ✅ Deploy confirmado em producao / ✅ Manutencao fechada

---

### [2026-08-05] — Manutencao #23 — Fix path duplicado na busca de produto e cliente no PDV

**Tarefas executadas:**
- Corrigido path duplicado que causava 404 sempre nas buscas do PDV
- GET /api/v1/produtos/?search=... e GET /api/v1/clientes/?search=... passaram a retornar 200 com resultados reais

**Arquivos alterados:**
- (path duplicado corrigido nas rotas/views do PDV — identificado pelo Sentinel com teste real nos endpoints)

**Commits:**
- 262bc31 — fix(pdv): corrige path duplicado na busca de produto e cliente - Manutencao 23
- 0107426 — docs: registra deploy Manutencao 23 no CLAUDE.md (fix path duplicado PDV)
- 082f8af — docs: registra Especificacao_Hotfix retroativa da Manutencao 23 (fix path duplicado PDV)

**Sentinel:**
- 182/182 testes Django passando (0 falhas)
- GET /api/v1/produtos/?search=Rel → 200, produto real retornado
- GET /api/v1/clientes/?search=Maria → 200, cliente real retornado
- Status: APROVADO

**Deploy:**
- Data: 2026-08-05 19:01:05 UTC (16:01 -03)
- Metodo: git push origin main → CI/CD GitHub Actions (.github/workflows/deploy.yml)
- Push confirmado: eef6c69..262bc31 main -> main

**Fechamento formal do ciclo (Pilot, 2026-08-05 ~19:38 UTC):**
- Estado ao assumir: apenas `Especificacao_Hotfix.md` com alteracoes locais
  nao commitadas (documento retroativo da Manutencao 23, escrito pelo
  Sentinel/Analista como roteiro de verificacao); `backend/test_whitelist_pdv.py`
  untracked ignorado por instrucao explicita (residuo da Manutencao 22)
- Commit: 082f8af — apenas `Especificacao_Hotfix.md`
- Push confirmado: 0107426..082f8af main -> main
- CI/CD disparou automaticamente: `uidcore-backend-1` recriado 2026-08-05T19:38:30Z,
  poucos segundos apos o push — deploy real confirmado, sem intervencao manual
- Health check: HTTP 200 na raiz, HTTP 401 em `/api/v1/financeiro/dashboard/` (esperado, requer auth)
- SystemD: Manutencao #23 marcada `feito=True` via `disparar_hotfix --concluir 23`
  (1 notificacao IMPEDIMENTO_ESTEIRA resolvida junto)
- URL: http://uidcore.uidsoftware.com.br (porta interna 8006)
- Status: ✅ Aprovado por Sentinel / ✅ Deploy confirmado em producao / ✅ Manutencao fechada
- Container uidcore-backend-1 recriado as 2026-08-05T19:01:05Z (16:01 -03)
- Health check: HTTP 401 em /api/v1/financeiro/dashboard/ (esperado, requer auth)
- SystemD: Manutencao #23 marcada feito=True via disparar_hotfix --concluir 23
- URL: http://uidcore.uidsoftware.com.br (porta interna 8006)
- Status: ✅ Aprovado por Sentinel / ✅ Deploy confirmado em producao

---

### [2026-08-05] — Manutencao #22 — Fix PATCH 405 em VendaViewSet (vincular cliente + editar item PDV)

**Tarefas executadas:**
- Fix PATCH 405 em VendaViewSet: acoes `vincular_cliente` e `editar_item` nao aceitavam PATCH (metodo nao declarado)
- Whitelist de campos protegidos no PATCH generico: tentativa de alterar status/operador via PATCH e bloqueada
- vincular/desvincular cliente confirmado no banco (Sentinel testou de verdade)
- editar quantidade de item: valor_total recalculado corretamente

**Arquivos alterados:**
- backend/pdv/views.py (VendaViewSet — declaracao PATCH + whitelist)
- backend/pdv/tests.py (testes cobrindo vincular, desvincular, editar, whitelist)

**Commits:**
- 03a85a9 — fix(pdv): PATCH 405 em vincularCliente e editarItem — Manutencao 22

**Sentinel:**
- 182/182 testes Django passando (0 falhas)
- vincular/desvincular cliente confirmado no banco
- editar quantidade com valor_total recalculado confirmado
- whitelist bloqueando PATCH de campos protegidos (status/operador) confirmada
- Status: APROVADO

**Deploy:**
- Data: 2026-08-05 17:33:58 UTC (14:33 -03)
- Metodo: git push origin main → CI/CD GitHub Actions (.github/workflows/deploy.yml)
- Push confirmado: 0d96a8b..03a85a9 main -> main
- Container uidcore-backend-1 recriado as 2026-08-05T17:33:56Z (14:33 -03)
- Gunicorn: 3 workers up, sem crash
- Health check: HTTP 401 em /api/v1/financeiro/dashboard/ (esperado, requer auth)
- SystemD: Manutencao #22 marcada feito=True via disparar_hotfix --concluir 22
- URL: http://uidcore.uidsoftware.com.br (porta interna 8006)
- Status: ✅ Aprovado por Sentinel / ✅ Deploy confirmado em producao

---

### [2026-08-05] — Manutencao #21 — Ajustes PDV pos-lancamento (RF-17 a RF-23)

**Tarefas executadas:**
- RF-17: Enter no campo de busca da FrenteDeCaixa com match exato de codigo_barras adiciona produto ao carrinho sem clique (+ fix de race condition com debounce cancelado)
- RF-18/19/20: ModalScannerCamera.jsx — botao camera, leitura via @zxing/library, permissao negada trata com toast
- RF-21/22: AberturaCaixa exibe operador logado + data/hora atual + label Fundo de Troco
- RF-23: bug sessao_caixa — criarVenda() redireciona para /pdv/abertura com mensagem amigavel em vez de erro cru
- Grupo 5 UI: Card Carrinho com altura reduzida + SplitPagamento com espacamento aumentado
- Fix pontual pos-Sentinel: RF-17 leitor fisico de codigo de barras nao adicionava produto ao carrinho

**Arquivos alterados:**
- frontend/src/pages/pdv/FrenteDeCaixa.jsx
- frontend/src/pages/pdv/AberturaCaixa.jsx
- frontend/src/pages/pdv/components/SplitPagamento.jsx
- frontend/src/pages/pdv/components/ModalScannerCamera.jsx (novo)
- frontend/package.json + package-lock.json (@zxing/library adicionada)

**Commits:**
- a762efb — feat(frontend): ajustes PDV pos-lancamento (RF-17 a RF-23) - Manutencao 21
- 3d649d4 — fix(pdv): RF-17 leitor fisico de codigo de barras nao adicionava produto ao carrinho

**Sentinel:**
- 179/179 testes Django passando (0 falhas, backend nao alterado)
- npm run build limpo (2163 modules, 0 erros)
- 8/8 criterios de aceite verificados
- Status: APROVADO

**Deploy:**
- Data: 2026-08-05 16:34:53 UTC (13:34 -03)
- Metodo: git push origin main → CI/CD GitHub Actions (.github/workflows/deploy.yml)
- Push confirmado: origin/main em 3d649d4
- Container uidcore-backend-1 recriado as 2026-08-05T16:34:53Z
- SystemD: Manutencao #21 marcada feito=True via disparar_hotfix --concluir 21
- URL: http://uidcore.uidsoftware.com.br (porta interna 8006)
- Status: ✅ Aprovado por Sentinel / ✅ Deploy confirmado em producao

---

### [2026-08-05] — Manutencao #15 — Modulo PDV completo

**Tarefas executadas:**
- RF-01 a RF-16: modulo PDV implementado do zero (backend + frontend)
- Backend: app `pdv` criada com models SessaoCaixa, VendaPDV, ItemVendaPDV, EstornoReceita; migrations geradas; testes unitarios (22 testes cobrindo todos os RFs)
- Frontend: telas AberturaCaixa, FrenteDeCaixa, FechamentoCaixa, HistoricoVendas, RelatorioSessoes; componentes; rotas; item de sidebar
- Fix pontual pos-Sentinel: limpeza de prefetch cache stale em finalizar e cancelar venda

**Arquivos alterados:**
- backend/pdv/ (app completa: models, serializers, views, urls, signals, tests, admin)
- backend/config/settings.py (pdv adicionado em INSTALLED_APPS)
- backend/config/urls.py (rotas pdv registradas)
- frontend/src/pages/ (AberturaCaixa, FrenteDeCaixa, FechamentoCaixa, HistoricoVendas, RelatorioSessoes)
- frontend/src/routes/index.jsx
- frontend/src/components/layout/Sidebar.jsx

**Commits:**
- 69b4c6e — feat(frontend): PDV Manutencao 15 — HistoricoVendas, RelatorioSessoes, rotas, sidebar
- 95e30b1 — feat(backend): PDV Manutencao 15 — EstornoReceita, fiacao pdv app, migrations, tests
- ccc2b3e — feat(frontend): PDV Manutencao 15 — telas AberturaCaixa, FrenteDeCaixa, FechamentoCaixa e componentes
- b01eb4f — fix(pdv): limpa prefetch cache stale em finalizar e cancelar venda

**Sentinel:**
- 179/179 testes passando (0 falhas)
- 22 testes do modulo pdv cobrindo RF-01 a RF-16
- 0 regressao em modulos existentes
- Status: APROVADO

**Deploy:**
- Data: 2026-08-05 14:43 UTC (11:43 -03)
- Metodo: git push origin main → CI/CD GitHub Actions (.github/workflows/deploy.yml)
- Push confirmado: bbbd27e..b01eb4f main -> main
- Container uidcore-backend-1 recriado as 2026-08-05T14:42:50Z
- Health check: HTTP 401 em /api/v1/financeiro/dashboard/ (esperado, requer auth)
- SystemD: Manutencao #15 marcada feito=True via disparar_hotfix --concluir 15
- URL: http://uidcore.uidsoftware.com.br (porta interna 8006)
- Status: ✅ Aprovado por Sentinel / ✅ Deploy confirmado em producao

---

### [2026-08-03] — Manutencao #13 — Correcao de 3 bugs contabeis de CARTEIRA

**Tarefas executadas:**
- Bug 1: _saldo_total_contas() incluia contas do tipo CARTEIRA no saldo disponivel, inflando o patrimonio — corrigido com filtro excludindo CARTEIRA
- Bug 2: calcular_balanco() nao calculava nem exibia a divida de cartao de credito — corrigido com nova funcao _divida_cartao_credito() e inclusao em passivo.circulante
- Bug 3: NameError em views.py linha 675 (indicadores['saldo_total'] referenciava variavel inexistente) — corrigido para o campo correto

**Arquivos alterados:**
- backend/financeiro/relatorios.py (_saldo_total_contas exclui CARTEIRA, nova _divida_cartao_credito, calcular_balanco inclui cartao_credito_a_pagar)
- backend/financeiro/views.py (linha 675 corrigida, bloco inline morto removido)
- backend/financeiro/tests.py (9 novos testes CA-01 a CA-06)

**Commits:**
- 47c7830 — fix(financeiro): corrige 3 bugs contabeis de CARTEIRA - Manutencao 13
- 1fa75a2 — fix(financeiro): corrige NameError saldo_total e teste delete CARTEIRA - Manutencao 13

**Sentinel:**
- 157 testes passando (0 falhas)
- 9/9 criterios de aceite verificados (CA-01 a CA-06 + testes adicionais)
- Status: APROVADO

**Deploy:**
- Data: 2026-08-03 21:31:36 UTC (18:31 -03)
- Metodo: git push origin main → CI/CD GitHub Actions (.github/workflows/deploy.yml)
- Push confirmado: 450ce11..1fa75a2 main -> main
- Container uidcore-backend-1 recriado as 2026-08-03T21:31:36Z
- Health check: HTTP 200 na raiz, HTTP 401 em /api/v1/financeiro/dashboard/ (esperado, requer auth)
- URL: http://uidcore.uidsoftware.com.br (porta interna 8006)
- Status: Aprovado por Sentinel / Deploy confirmado em producao

---

### [2026-07-31] — Manutencao #12 — Correcao de autocomplete de produto em Orcamento e Pedido

**Tarefas executadas:**
- Fix de autocomplete de produto nas telas de Orcamento e Pedido (puramente frontend)
- Backend nao alterado nesta manutencao

**Arquivos alterados:**
- frontend (componentes de Orcamento e Pedido — autocomplete de produto)

**Commit:**
- 8e32224 — fix(frontend): corrige autocomplete de produto em Orcamento e Pedido - Manutencao 12

**Sentinel:**
- 148 testes Django passando (0 falhas)
- npm run build limpo (1942 modules, 0 erros)
- 4/4 criterios de aceite atendidos
- Status: APROVADO

**Deploy:**
- Data: 2026-07-31 17:39:59 UTC (14:39 -03)
- Metodo: git push origin main → CI/CD GitHub Actions (.github/workflows/deploy.yml)
- Push confirmado: 688ce98..8e32224 main -> main
- Container uidcore-backend-1 recriado as 2026-07-31T17:39:59Z
- Health check: HTTP 200 na raiz, HTTP 401 em /api/v1/financeiro/dashboard/ (esperado, requer auth)
- Fix e puramente frontend — bundle reconstruido pelo CI/CD a partir do commit 8e32224
- SystemD: Manutencao #12 nao possui ID proprio no banco (fix tratado como continuacao do ciclo #11, ID banco 11 ja marcado feito=True)
- URL: http://uidcore.uidsoftware.com.br (porta interna 8006)
- Status: Aprovado por Sentinel / Deploy confirmado em producao

### [2026-07-31] — Manutencao #11 — Fases A/B/C/D backend + frontend

**Tarefas executadas:**
- Fases A, B, C e D implementadas no backend e frontend
- 14 migrations geradas e aplicadas (0 pendentes pos-deploy)
- Frontend reconstruido com 1942 modules, 0 erros de build

**Commits:**
- 665fa2f — feat(backend): Manutencao #11 — Fases A/B/C/D backend
- 300c240 — feat(frontend): Manutencao 11 - Fases A/B/C/D

**Sentinel:**
- 148 testes unitarios + 48 testes de integracao passando (0 falhas)
- 14 migrations aplicadas
- 7/7 criterios de aceite atendidos
- npm run build limpo (1942 modules, 0 erros)
- Status: APROVADO

**Deploy:**
- Data: 2026-07-31 15:29 UTC (12:29 -03)
- Metodo: git push origin main → CI/CD GitHub Actions (.github/workflows/deploy.yml)
- Container uidcore-backend-1 recriado as 2026-07-31T15:29:30Z
- Bundle frontend: index-ZvF2W9qN.js (429 KB, Jul 31 15:29) — rebuild confirmado
- Health check: HTTP 200 na raiz, HTTP 401 em /api/v1/financeiro/dashboard/ (esperado)
- Migrations pendentes pos-deploy: 0
- Ponto critico: imagem Docker reconstruida pelo CI/CD a partir dos commits 665fa2f e 300c240 (nao imagem antiga)
- SystemD: Manutencao #11 marcada feito=True via disparar_hotfix --concluir 11
- URL: http://uidcore.uidsoftware.com.br (porta interna 8006)
- Status: Aprovado por Sentinel / Deploy confirmado em producao

---

### [2026-07-30] — Manutencao #10 — Tela de Conciliacao Bancaria (frontend)

**Tarefas executadas:**
- Tela de Conciliacao Bancaria implementada em React (historico de conciliacoes + upload de extrato + detalhe/confirmar item pendente + CRUD de Padroes Seguros)
- Rota /conciliacao registrada dentro do ProtectedRoute
- Item de menu "Conciliação" (🔄) adicionado na Sidebar logo apos "Financeiro" (emoji intencional — DIV-UI03 ja documentada como decisao de projeto, nao lucide-react)
- Backend nao foi alterado nesta manutencao (endpoints de conciliacao ja existiam, consumidos como estavam)

**Arquivos alterados:**
- frontend/src/pages/Conciliacao.jsx (novo)
- frontend/src/routes/index.jsx
- frontend/src/components/layout/Sidebar.jsx

**Commit:**
- 43a4fa8 — feat(frontend): tela de Conciliacao Bancaria - Manutencao 10

**Sentinel (relatorio completo):**
- Ambiente de teste isolado (docker compose -p uidcore-test, portas 8007/5435) subido e destruido sem afetar producao (8006)
- 97/97 testes Django passando (0 falhas) — backend nao alterado, validado integro pos-migrations
- Fluxo real de upload testado ponta a ponta com PDF real no formato do parser C6: POST /api/v1/financeiro/conciliacoes/upload/ → 201, 3 itens extraidos corretamente
- 401 sem token, 400 com mensagem legivel em campo obrigatorio faltando, 405 em tentativa de DELETE/POST direto em /conciliacoes/{id}/ (ReadOnlyModelViewSet) — todos confirmados
- CRUD completo de Padroes Seguros testado via API real (criar ENTRADA/SAIDA, listar, editar PATCH, excluir = soft delete)
- Confirmar item FALTANDO_SISTEMA testado via API real (contador de divergencias_restantes cai de 3 para 2)
- Revisao linha a linha: serializers batendo com o consumo do frontend, RN-01 (campo natureza oculto quando tipo=SAIDA), RN-02 (botao Confirmar so em FALTANDO_SISTEMA && !confirmado), RNF-03 (responsivo mobile/desktop), RNF-04 (.catch + extractErrorMessage em toda chamada), RNF-05 (vite build limpo, 1941 modules, 4.32s)
- 11/11 Criterios de Aceite (CA-01 a CA-11) do Especificacao_Hotfix.md verificados e passando
- **Status: APROVADO — 0 falhas**

**Deploy:**
- Metodo: git push origin main → CI/CD GitHub Actions (.github/workflows/deploy.yml) → SSH do runner do GitHub na VPS, zero SSH manual do Pilot
- Push confirmado: a72f371..43a4fa8 main -> main
- Deploy confirmado ponta a ponta (diagnostico pos-push, sem intervencao manual): container uidcore-backend-1 recriado as 2026-07-30 09:17:52 UTC (06:17 -03), HEAD do repo na VPS em 43a4fa8, bundle do frontend servido pelo nginx contem a tela nova (grep -i conciliacao nos assets), health check HTTP 200 na raiz e HTTP 401 (esperado, requer auth) no endpoint do dashboard
- URL: http://uidcore.uidsoftware.com.br (porta interna 8006 — verificar vhost/dominio definitivo no nginx-proxy)
- Status: ✅ Aprovado por Sentinel / ✅ Deploy confirmado em producao

### [2026-07-30] — Manutencao #9 — Correcao de divergencias pos-Blueprint/Brush

**Tarefas executadas:**
- DIV01: confirmado que pasta conciliacao/ nao existe — nenhuma acao necessaria
- DIV02: Dashboard.jsx integrado com endpoint real GET /api/v1/financeiro/dashboard/
- DIV04: AcessoPortalCliente e PadraoSeguroConciliacao padronizados para BaseModel (ativo→is_active, criado_em→created_at)
- DIV05: MetodoPagamento e Agenda sem campo ativo proprio (is_active herdado do BaseModel)
- DIV07: 9 serializers padronizados com id = IntegerField(source=pk, read_only=True) como primeiro campo
- DIV08: HistoricoCliente e ItemConciliacao herdam BaseModel (created_at, updated_at, is_active)
- DIV-UI01: fontes Plus Jakarta Sans + DM Sans configuradas (index.html, tailwind.config.js, index.css)
- DIV-UI02: overflow-hidden removido do root do AppLayout
- DIV-UI04: lucide-react confirmado e persistido no package.json
- Suspenso: DIV03 (Portal CLIENTE), DIV10 (endpoint usuarios), DIV-UI03 (emojis Sidebar)

**Commits:**
- e74774f — fix(models): padroniza BaseModel e serializers (DIV04/05/07/08) - Manutencao 9
- 21636cb — fix(frontend): fontes Uid, overflow-hidden, Dashboard real (DIV-UI01/02/DIV02) - Manutencao 9
- 398b407 — fix(service+tests): atualiza referencias de campo pos-BaseModel - Manutencao 9
- e05845b — docs: persiste Especificacao_Hotfix da Manutencao 9

**Migrations criadas (5 apps):**
- portal/0003_portal_basemodel.py + 0004_portal_basemodel_cleanup.py
- agendamento/0003_agenda_remove_ativo.py
- financeiro/0004_financeiro_basemodel.py + 0005_financeiro_basemodel_cleanup.py
- clientes/0003_historico_cliente_basemodel.py + 0004_historico_basemodel_cleanup.py
- pagamentos/0003_metodo_pagamento_remove_ativo.py

**Sentinel:** APROVADO (23 CAs verificados, 0 issues, container rebuiltado)

**Deploy:**
- Data: 2026-07-30
- Metodo: git push origin main → CI/CD GitHub Actions
- URL: http://uidcore.uidsoftware.com.br (ou verificar no nginx-proxy)
- Status: entregue ao CI/CD

---

### [2026-07-28] — Manutenção #8 — Reexecução completa do Fluxo 1 (todos os agents)

**Contexto:** Fluxo 1 havia rodado incompleto na primeira vez (direto Forge+Loom
sem Analista, doc-generator, Blueprint, Brush). Esta execução refez o pipeline
completo retroativamente, documentando o sistema já em produção. Nenhum código
foi alterado — apenas documentação foi gerada e registrada.

**Agents executados:**
- Analista: Levantamento_Requisitos.md + usecase.md + classes.md + activity.md
- doc-generator: 8 documentos em /docs/ (01_visao_geral ate 08_glossario)
- Blueprint: Blueprint.md (671 linhas, 35 endpoints, 14 ADRs) + ADRs.md (542 linhas)
- Brush: design_system.md (622 linhas, 6 divergencias UI identificadas)
- Sentinel: APROVADO — 38 migrations, 33 models, 15 artefatos confirmados

**Artefatos registrados no SystemD (AT#2):**
- IDs 8-11: Analista (levantamento_requisitos, uml_usecase, uml_classes, uml_activity)
- IDs 12-19: doc-generator (8 documentos estruturais)
- ID 20: Brush (design_system)
- IDs 21-22: Blueprint (blueprint, ADRs)
- Total: 15 artefatos vinculados a ArquiteturaTecnica#2

**Arquivos gerados em disco:**
- /var/www/uidcore/Levantamento_Requisitos.md (18.7 KB)
- /var/www/uidcore/usecase.md (5.6 KB)
- /var/www/uidcore/classes.md (11.1 KB)
- /var/www/uidcore/activity.md (10.1 KB)
- /var/www/uidcore/Blueprint.md (29 KB)
- /var/www/uidcore/ADRs.md (17.9 KB)
- /var/www/uidcore/design_system.md (24 KB)
- /var/www/uidcore/docs/ (8 arquivos: 01 a 08)

**Divergencias documentadas (10 itens, nenhuma bloqueante):**
- DIV01: pasta conciliacao/ vazia no disco
- DIV02: Dashboard.jsx com metricas estaticas
- DIV03: Portal sem telas para perfil CLIENTE
- DIV04/05: campos ativo vs is_active inconsistentes em 3 models
- DIV07: serializers usando read_only_fields em vez de IntegerField(source=pk)
- DIV08: 2 models sem herdar BaseModel
- DIV10: endpoint de listagem de usuarios ausente
- Brush: fontes Uid (Plus Jakarta Sans + DM Sans) ausentes no frontend
- Brush: overflow-hidden no AppLayout root (violacao de regra global)
- Brush: emojis na Sidebar em vez de Lucide icons

**Status:** Documentacao completa gerada e validada. Sistema em producao inalterado.
Proxima etapa: corrigir divergencias na Manutencao #9 (a priorizar com Luiz Eduardo).

---

### [2026-07-27] — Manutenção #7 (Fases D+E) fechada de vez + limpeza de branches órfãs

**Contexto:** Fases D (conciliação bancária) e E (Vendas, Pagamentos, RH,
Agendamento, Administrativo, Portal do Cliente) foram implementadas e
deployadas em 26/07 (commits `168c332` backend, `f47cea2` frontend). O
disparo automático via `disparar_hotfix.py` tinha esgotado 3 tentativas em
23/07 (bloqueio recorrente do classificador de segurança do Claude Code em
sessões `-p` sem TTY) — o usuário autorizou explicitamente concluir **sem
esteira**: trabalho continuado direto, commitado em `main`, CI/CD real
disparou sozinho a cada push.

**Commits desta rodada final (25-27/07):**
- `168c332` — backend completo Fases D+E (conciliação + 6 módulos)
- `f47cea2` — frontend completo dos 6 módulos + 2 bugs reais (boolean
  ausente em multipart vira `False`; `CobrancaViewSet`/`DocumentoViewSet`
  só aceitavam multipart, rejeitavam JSON puro)
- `b92d039` — fix: migration faltando pro campo `nome_razao_social` opcional
- `d26e3e5` — feat: todos os campos opcionais exceto conta no Financeiro
- `4579225` — fix: mensagem real de erro da API em vez de texto genérico
- `e8a0567` — fix: remove strings vazias do payload antes de enviar

**Confirmado deployado:** timestamp de criação do container
`uidcore-backend-1` e do build do frontend servido pelo nginx batem
exatamente com o horário do commit `e8a0567` (27/07 01:06 -03) — CI/CD
rodou sozinho, sem intervenção manual de deploy.

**O que ficou pendurado e foi limpo em 27/07 (ajuste de estado/higiene,
fora do escopo de código de produto):**
- `Manutencao.feito` continuava `False` no banco do SystemD mesmo com tudo
  em produção — handoff automático Sentinel→Pilot nunca fechou o ciclo
  (mesmo padrão recorrente já visto nas rodadas anteriores desta
  manutenção). Marcado `feito=True` manualmente.
- 3 branches `climpire/*` órfãs (`28b7159d`, `7c176f53`, `8889a134`) e os
  worktrees correspondentes em `.climpire-worktrees/` — todas eram
  ancestrais de `main` sem nenhum commit único (o trabalho real foi
  commitado direto em `main`, não nessas branches) — deletadas.
- Diff pendente em `frontend/package.json`/`package-lock.json` (só
  reordenação alfabética de dependências, sem mudança de versão real) —
  descartado.

**Regra pra próxima vez que a esteira travar numa manutenção multi-fase:**
se o usuário autorizar concluir sem esteira, confirmar sempre o deploy
real (timestamp do container/build batendo com o commit) antes de
considerar pronto, e lembrar de marcar `feito=True` manualmente no banco —
o pipeline automático não faz isso sozinho quando o trabalho final não
passou por Sentinel/Pilot formalmente.

---

## Padrões financeiros herdados do SystemD (aplicar desde o início, não só corrigir depois)

Regras completas e o porquê em `/home/notuidsoftware/.claude/CLAUDE.md`
(seção Módulo Financeiro — vale pra qualquer projeto Uid com livro caixa,
não só SystemD/UidCore). Resumo já adaptado pra nomenclatura do UidCore
(`is_active`, não `ativo` como no SystemD — `BaseModel` em `common/models.py`):

- **Cartão de crédito é uma `Conta` própria** (`tipo=CARTEIRA`, já existe em
  `TipoConta.choices`), nunca lançamento genérico direto no banco. Compra
  categorizada entra nela (Ferramentas, Impostos/IOF etc.). O pagamento da
  fatura no banco real TEM que creditar a conta do cartão — usar
  "Transferir entre contas" banco→cartão pelo valor pago, nunca só uma
  Despesa genérica marcada `is_active=False` no banco. `is_active=False`
  sozinho corrige o DRE mas deixa a conta do cartão sem saber que foi
  paga, inflando a "dívida" artificialmente (bug real encontrado e
  corrigido no SystemD em 29/07/2026 — 4 pagamentos históricos lançados
  assim deixaram o cartão R$400+ mais negativo do que a realidade).
- **CDB/aplicação rende sozinho** — a Conta de Aplicação/Garantia não
  fica parada no valor investido, o banco credita rendimento com o
  tempo. Conferir periodicamente o saldo bruto real no app do banco
  contra o saldo calculado no sistema; a diferença é rendimento não
  lançado — registrar como Receita (`tipo=RECEITA_FINANCEIRA`,
  `categoria=Investimento`, `is_active=True` — ENTRA no DRE, diferente
  de aporte/transferência) na própria Conta de Aplicação.
- **Fluxo de Caixa/Livro Caixa/Contas Bancárias vs Resultado (DRE) vs
  DFC** — três coisas diferentes: Fluxo de Caixa, Livro Caixa e saldo de
  Contas Bancárias TÊM que bater com o extrato real do banco sempre
  (incluindo transferência entre contas próprias e aplicação/resgate).
  Resultado do mês/DRE é operacional — soma só Receita/Despesa com
  `is_active=True`, nunca soma LivroCaixa bruto; aporte e transferência
  nunca entram aí. DFC (se vier a existir) é analítico, não precisa
  bater linha a linha com o banco.
- **Transferência entre bolsos do mesmo banco** (conta corrente ↔
  aplicação/CDB/conta remunerada, liquidez D+0/D+1) não é despesa nem
  receita — `is_active=False` na Despesa/Receita, mas o `LivroCaixa`
  continua mostrando o movimento real (não filtra por `is_active`, só por
  `estornado`).
- **Aporte ≠ Receita Financeira** — aporte de sócio é Patrimônio Líquido,
  nunca entra no DRE; rendimento de aplicação automática é Receita
  Financeira, separada da receita operacional.
- **Gasto em moeda estrangeira (USD) no cartão** — nunca recorrência fixa
  (`recorrente`/`frequencia`/`quantidade`). Câmbio varia mês a mês —
  lançar com o valor real da fatura quando ela chegar.
- **Estorno sempre em par** — `LivroCaixaViewSet`/`DespesaViewSet.estornar`
  têm que marcar `estornado=True` nos DOIS lados (lançamento original E o
  de reversão), senão duplica o efeito na soma em vez de neutralizar (bug
  real já corrigido tanto no SystemD quanto na Fase B do próprio UidCore —
  ver histórico de Manutenção #7 acima).

**Confirmado em 28/07/2026** (checagem de arquitetura, sem código): `Conta.
tipo` já tem `CARTEIRA`, `Despesa`/`Receita`/`Conta` já herdam `is_active`
de `BaseModel`, e os ViewSets já filtram `is_active=True` por padrão — a
base está pronta, só falta usar certo desde o primeiro cliente que tiver
cartão de crédito no fluxo (não esperar acumular lançamento genérico
errado pra corrigir depois).

---

## Complemento: cartão com garantia (CDB/aplicação) — cadeia de 3 contas

Refina o padrão de cartão de crédito documentado acima. Quando o cartão do
cliente tem limite por garantia (comum em cartão de banco digital tipo C6
Business — dinheiro aplicado em CDB destrava o limite), usar **3 `Conta`
encadeadas**, nunca só banco+cartão:

```
Banco (Conta Corrente)  →  Aplicação/Garantia (Conta Poupança)  →  Cartão (Carteira Digital)
```

Cada perna é uma **transferência entre contas** (endpoint dedicado, nunca
`Despesa`) — aplicar em CDB não é gasto nem sai do patrimônio do cliente,
é só troca de forma; usar o CDB pra quitar fatura é transferência
Aplicação→Cartão, não um pagamento novo. Sem a conta intermediária, o
dinheiro aplicado como garantia "some" do sistema — nenhuma tela mostra
quanto está garantindo o limite. Regra completa (e o porquê, com caso real
resolvido) em `/home/notuidsoftware/.claude/CLAUDE.md`, Módulo Financeiro.

**Cuidado técnico ao portar o endpoint de transferência pro UidCore:** se
a implementação calcular `saldo_anterior`/`saldo_atual` lendo o ÚLTIMO
lançamento por data (em vez de recalcular por soma agregada), qualquer
transferência com data retroativa deixa o saldo gravado temporariamente
errado — precisa de um comando tipo `reconstruir_saldo_livro_caixa`
(existe no SystemD) rodado depois de qualquer lançamento fora de ordem
cronológica. Vale conferir se esse mesmo padrão de cálculo foi copiado pro
UidCore e, se sim, portar o comando de reconstrução junto.

---

### [2026-07-28] — Fluxo 1 refeito por completo (Manutenção #8): Analista, doc-generator, Blueprint e Brush

**Contexto:** o primeiro Fluxo 1 do UidCore (17-18/07) tinha pulado direto
de Arquitetura Técnica pra Forge/Loom, sem Analista/doc-generator/
Blueprint/Brush — zero artefatos desses 4 agents existiam no banco
(achado ao investigar por que "sentia falta dos produtos do Analista").
Criada Manutenção #8 com instrução explícita pra rodar todos os agents
faltantes, sem reescrever o código já em produção salvo divergência real
encontrada.

**Duas tentativas de disparo antes de dar certo:**
1. Cron disparou headless (`disparar_hotfix.py`) e bateu no bloqueio de
   sempre do classificador de segurança (Stage 2 classifier error) —
   comportamento esperado, o fallback criou a notificação
   `IMPEDIMENTO_ESTEIRA` corretamente, idempotente.
2. Terminal ao vivo aberto na notificação errada por engano (uma antiga,
   #6, sobre a Manutenção #7 já resolvida) — rodou uma re-confirmação
   inócua em vez do Fluxo 1. Sessão certa (notificação #8) rodou depois
   e completou.

**Resultado confirmado no banco:** 15 artefatos novos, todos vinculados
corretamente a `ArquiteturaTecnica#2` (não mais retroativos sem vínculo
como os 7 do SystemD): Analista (`levantamento_requisitos`, `uml_usecase`,
`uml_classes`, `uml_activity`), doc-generator (8 documentos), Brush
(`design_system`), Blueprint (`adr` + outro). Consultável em
Office → Artefatos, filtrando por UidCore. Manutenção #8 marcada
`feito=True`.

**Observação:** essa rodada não gerou commit novo no repo (não havia
mudança de código a fazer, por instrução explícita) — por isso não tem
entrada de "Pilot fez deploy" aqui; o registro é só de documentação
produzida.
